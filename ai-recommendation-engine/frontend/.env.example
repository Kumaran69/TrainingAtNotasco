# Leave unset for local dev (Vite proxies /api to localhost:5000).
# For a Netlify deploy, set this in Netlify's Site settings -> Environment
# variables to your backend's Render/Railway URL, no trailing slash:
# VITE_API_BASE_URL=https://course-recommender-api.onrender.com
VITE_API_BASE_URL=
